BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[submissions] ADD [ticketNumber] NVARCHAR(100) NOT NULL CONSTRAINT [submissions_ticketNumber_df] DEFAULT '';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
